/* book("The Magician's Nephew");

function book(title) {
  console.log(`The book is called ${title}`); // the book is called Magician's Nephew
} */

let book = "The Silver Chair"; // can't access book before initialization, put the call after initialization

console.log(book);
